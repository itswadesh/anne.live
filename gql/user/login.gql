mutation login($email: String!, $password: String!) {
  login(email: $email, password: $password) {
    id
    email
    firstName
    lastName
    city
    phone
    avatar
    role
    verified
    active
    provider
    createdAt
    updatedAt
  }
}
