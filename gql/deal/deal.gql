query deal($id: ID!) {
  deal(id: $id) {
    id
    name
    img
    startTime
    endTime
    startTimeISO
    endTimeISO
    onGoing
    products {
      id
      name
      img
      slug
    }
    dealStatus
    active
  }
}
